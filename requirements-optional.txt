# Optional dependencies for autoresearch
# Install only if you need these features

# PDF support - for reading .pdf files in inputs
pypdf>=3.0.0

# DOCX support - for reading .docx files in inputs
python-docx>=0.8.11

# Instructor - for more robust structured output (if supported by the model)
# instructor>=1.0.0